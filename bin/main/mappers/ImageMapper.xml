<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pikcurchu.pikcur.mapper.ImageMapper">
    <insert id="insertGoodsImage">
        insert into image(goods_id, image_path, sort) values(#{goodsId}, #{imageUrl}, #{sort})
    </insert>
    <select id="findGoodsImages">
        select image_path
        from image
        where goods_id = #{goodsId}
    </select>
</mapper>